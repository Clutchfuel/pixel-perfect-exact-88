interface SEOHeadProps {
  title?: string;
  description?: string;
  canonical?: string;
  ogImage?: string;
}

// Placeholder component — props only. Wired up later in Cursor.
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function SEOHead(_props: SEOHeadProps) {
  return null;
}
